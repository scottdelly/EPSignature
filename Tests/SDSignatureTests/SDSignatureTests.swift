import XCTest
@testable import SDSignature

class SDSignatureTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SDSignature().text, "Hello, World!")
    }


    static var allTests = [
        ("testExample", testExample),
    ]
}
